{
  "scripts": {
    "save": "git add . && git commit -m 'save' && git push origin"
  },
  "dependencies": {
    "ewiki": "^0.0.50",
    "markdown-it-anchor": "^9.0.1",
    "markdown-it-container": "^4.0.0"
  }
}
